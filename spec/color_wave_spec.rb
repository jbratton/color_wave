require 'spec_helper'

describe ColorWave do
  it 'has a version number' do
    expect(ColorWave::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
